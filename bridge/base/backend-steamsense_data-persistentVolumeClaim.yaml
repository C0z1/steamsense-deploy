#! backend-steamsense_data-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: backend-steamsense-data
    namespace: steamsense-final
    labels:
        com.docker.compose.service: backend
        com.docker.compose.volume: steamsense_data
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
